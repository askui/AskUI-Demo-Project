# Open Notepad

1. Open the Notepad application (or the default text editor on your system).
2. Wait until the editor window is fully loaded and visible.
3. Confirm the editor is ready by verifying an empty document is shown.
