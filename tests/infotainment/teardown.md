# Infotainment SIL Teardown

1. Navigate back to the Home screen if not already there (tap the Home button in the emulator)
2. Use the `sil_mock_service` tool to call `POST reset` to reset all mock data to fixture defaults
3. Leave the emulator running (it will be reused or shut down via `start_sil.sh`)
