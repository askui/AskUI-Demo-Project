# Navigation Testing Rules

- All navigation tests begin from the Navigation screen
- When entering addresses or POI searches, use keyboard input after tapping the search/input field
- Wait for map rendering and route calculations to complete before verifying results
- The emulator may use simulated GPS data — accept any valid-looking coordinates or map position
