# Media Testing Rules

- All media tests begin from the Audio/Media screen
- When switching audio sources, wait for the source to fully load before verifying playback
- Volume level changes should be verified by observing the on-screen volume indicator
- If a media source is unavailable in the emulator, mark the step as SKIPPED with a note
